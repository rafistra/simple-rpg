const HeroController = require('./HeroController');
const ClassController = require('./ClassController');
const PartyController = require('./PartyController');

module.exports = {
    HeroController, ClassController, PartyController
}